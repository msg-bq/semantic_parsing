$START=$ASSERTION

$ASSERTION=Coordinate ( $POINT ) equals ( $NUMBER ,  $NUMBER )
|XCoordinate ( $POINT ) equals $NUMBER
|YCoordinate ( $POINT ) equals $NUMBER
|LocusEquation ( $POINT ) equals $EXPRESSION
|Locus ( $POINT ) equals $CURVE
|Quadrant ( $POINT ) equals $NUMBER
|Focus ( $CONICSECTION ) equals $POINT
|RightFocus ( $CONICSECTION ) equals $POINT
|LeftFocus ( $CONICSECTION ) equals $POINT
|LowerFocus ( $CONICSECTION ) equals $POINT
|UpperFocus ( $CONICSECTION ) equals $POINT
|FocalLength ( $CONICSECTION ) equals $NUMBER
|HalfFocalLength ( $CONICSECTION ) equals $NUMBER
|SymmetryAxis ( $CONICSECTION ) equals $AXIS
|Eccentricity ( $CONICSECTION ) equals $NUMBER
|Vertex ( $CONICSECTION ) equals $POINT
|UpperVertex ( $CONICSECTION ) equals $POINT
|LowerVertex ( $CONICSECTION ) equals $POINT
|LeftVertex ( $CONICSECTION ) equals $POINT
|RightVertex ( $CONICSECTION ) equals $POINT
|Directrix ( $CONICSECTION ) equals $LINE
|LeftDirectrix ( $CONICSECTION ) equals $LINE
|RightDirectrix ( $CONICSECTION ) equals $LINE
|MajorAxis ( $ELLIPSE ) equals $LINESEGMENT
|MinorAxis ( $ELLIPSE ) equals $LINESEGMENT
|RealAxis ( $ELLIPSE ) equals $LINESEGMENT
|ImaginaryAxis ( $ELLIPSE ) equals $LINESEGMENT
|LeftPart ( $HYPERBOLA ) equals $CURVE
|RightPart ( $HYPERBOLA ) equals $CURVE
|Asymptote ( $HYPERBOLA ) equals $LINE
|Diameter ( $CIRCLE ) equals $NUMBER
|Radius ( $CIRCLE ) equals $NUMBER
|Perimeter ( $CONICSECTION ) equals $NUMBER
|Center ( $CONICSECTION ) equals $POINT
|Expression ( $CURVE ) equals $EXPRESSION
|LineOf ( $POINT ,  $POINT ) equals $LINE
|Slope ( $LINE ) equals $NUMBER
|Inclination ( $LINE ) equals $NUMBER
|Intercept ( $LINE ,  $AXIS ) equals $NUMBER
|LineSegmentOf ( $POINT ,  $POINT ) equals $LINESEGMENT
|Length ( $LINESEGMENT ) equals $NUMBER
|MidPoint ( $LINESEGMENT ) equals $POINT
|OverlappingLine ( $LINESEGMENT ) equals $LINE
|PerpendicularBisector ( $LINESEGMENT ) equals $LINE
|Endpoint ( $LINESEGMENT ) equals $POINT
|Projection ( $POINT ,  $LINE ) equals $POINT
|InterceptChord ( $LINE ,  $CONICSECTION ) equals $LINESEGMENT
|TriangleOf ( $POINT ,  $POINT ,  $POINT ) equals $TRIANGLE
|IsInscribedCircle ( $CIRCLE ,  $TRIANGLE ) equals $BOOL
|CircumCircle ( $TRIANGLE ) equals $CIRCLE
|Incenter ( $TRIANGLE ) equals $POINT
|Orthocenter ( $TRIANGLE ) equals $POINT
|Circumcenter ( $TRIANGLE ) equals $POINT
|AngleOf ( $POINT ,  $POINT ,  $POINT ) equals $DEGREE
|VectorOf ( $POINT ,  $POINT ) equals $VECTOR
|DotProduct ( $VECTOR ,  $VECTOR ) equals $NUMBER
|NumIntersection ( $CURVE ,  $CURVE ) equals $NUMBER
|Intersection ( $CURVE ,  $CURVE ) equals $POINT
|FootPoint ( $LINE ,  $LINE ) equals $POINT
|Distance ( $POINT ,  $POINT ) equals $NUMBER
|Area ( $CONICSECTION ) equals $NUMBER
|TangentPoint ( $LINE ,  $CIRCLE ) equals $POINT
|TangentOfPoint ( $POINT ,  $CURVE ) equals $LINE
|TangentOnPoint ( $POINT ,  $CURVE ) equals $LINE
|Abs ( $LINESEGMENT ) equals $NUMBER
|lg ( $NUMBER ) equals $NUMBER
|InterReciprocal ( $NUMBER ) equals $NUMBER
|sqrt ( $NUMBER ) equals $NUMBER
|Max ( $NUMBER ) equals $NUMBER
|Min ( $NUMBER ) equals $NUMBER
|ApplyUnit ( $NUMBER ,  $DEGREESTR )
|Between ( $POINT ,  $POINT ,  $POINT ) equals $BOOL
|IsIntersect ( $CURVE ,  $CURVE ) equals $BOOL
|IsPerpendicular ( $LINE ,  $LINE ) equals $BOOL
|IsParallel ( $LINE ,  $LINE ) equals $BOOL
|IsDiameter ( $LINESEGMENT ,  $CIRCLE ) equals $BOOL
|IsChordOf ( $LINESEGMENT ,  $CONICSECTION ) equals $BOOL
|IsTangent ( $LINE ,  $CONICSECTION ) equals $BOOL
|IsInscribedCircle ( $CIRCLE ,  $TRIANGLE ) equals $BOOL
|IsCircumCircle ( $CIRCLE ,  $TRIANGLE ) equals $BOOL
|IsInTangent ( $CIRCLE ,  $CIRCLE ) equals $BOOL
|IsOutTangent ( $CIRCLE ,  $CIRCLE ) equals $BOOL
|PointOnCurve ( $POINT ,  $CURVE ) equals $BOOL
|And ( $ASSERTION ,  $ASSERTION ) equals $BOOL
|Negation ( $ASSERTION ) equals $BOOL

$POINT=POINT: capital_generate
|Center ( $CONICSECTION )
|UpperVertex ( $CONICSECTION )
|Projection ( $POINT ,  $LINE )
|UpperFocus ( $CONICSECTION )
|Incenter ( $TRIANGLE )
|LeftFocus ( $CONICSECTION )
|Orthocenter ( $TRIANGLE )
|MidPoint ( $LINESEGMENT )
|Circumcenter ( $TRIANGLE )
|LowerVertex ( $CONICSECTION )
|FootPoint ( $LINE ,  $LINE )
|RightVertex ( $CONICSECTION )
|RightFocus ( $CONICSECTION )
|LeftVertex ( $CONICSECTION )
|LowerFocus ( $CONICSECTION )

$NUMBER=NUMBER: number_generate
|Inclination ( $LINE )
|lg ( $NUMBER )
|Diameter ( $CIRCLE )
|HalfFocalLength ( $CONICSECTION )
|Min ( $NUMBER )
|Quadrant ( $POINT )
|InterReciprocal ( $NUMBER )
|XCoordinate ( $POINT )
|Radius ( $CIRCLE )
|NumIntersection ( $CURVE ,  $CURVE )
|Eccentricity ( $CONICSECTION )
|Area ( $CONICSECTION )
|YCoordinate ( $POINT )
|Length ( $LINESEGMENT )
|FocalLength ( $CONICSECTION )
|Slope ( $LINE )
|Perimeter ( $CONICSECTION )
|Abs ( $LINESEGMENT )
|Intercept ( $LINE ,  $AXIS )
|DotProduct ( $VECTOR ,  $VECTOR )
|Distance ( $POINT ,  $POINT )
|sqrt ( $NUMBER )

$EXPRESSION=EXPRESSION: expression_generate
|Expression ( $CURVE )
|LocusEquation ( $POINT )

$CURVE=CURVE: capital_generate
|RightPart ( $HYPERBOLA )
|LeftPart ( $HYPERBOLA )
|Locus ( $POINT )

$CONICSECTION=CONICSECTION: capital_generate

$AXIS=AXIS: xAxis
|AXIS: yAxis
|SymmetryAxis ( $CONICSECTION )

$LINE=LINE: capital_generate
|OverlappingLine ( $LINESEGMENT )
|RightDirectrix ( $CONICSECTION )
|PerpendicularBisector ( $LINESEGMENT )
|TangentOnPoint ( $POINT ,  $CURVE )
|LineOf ( $POINT ,  $POINT )
|LeftDirectrix ( $CONICSECTION )
|TangentOfPoint ( $POINT ,  $CURVE )

$ELLIPSE=ELLIPSE: capital_generate

$LINESEGMENT=LINESEGMENT: capital_generate
|ImaginaryAxis ( $ELLIPSE )
|MinorAxis ( $ELLIPSE )
|MajorAxis ( $ELLIPSE )
|LineSegmentOf ( $POINT ,  $POINT )
|RealAxis ( $ELLIPSE )
|InterceptChord ( $LINE ,  $CONICSECTION )

$HYPERBOLA=HYPERBOLA: capital_generate

$CIRCLE=CIRCLE: capital_generate
|CircumCircle ( $TRIANGLE )

$TRIANGLE=TRIANGLE: capital_generate
|TriangleOf ( $POINT ,  $POINT ,  $POINT )

$BOOL=BOOL: True
|BOOL: False

$DEGREE=AngleOf ( $POINT ,  $POINT ,  $POINT )
|ApplyUnit ( $NUMBER ,  $DEGREESTR )

$VECTOR=VECTOR: capital_generate
|VectorOf ( $POINT ,  $POINT )

$DEGREESTR=DEGREESTR: degree
